(function($) {
    $('.slider').slick({
        dots: false,
        arrows: false,
        infinite: true,
        speed: 500,
        fade: true,
        autoplay: true,
        autoplaySpeed: 8500,
        adaptiveHeight: true,
        draggable: false,
        cssEase: 'linear'
    });
}(jQuery));
